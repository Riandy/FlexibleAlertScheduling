<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.riandy.flexiblealertscheduling"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.riandy.flexiblealertscheduler.AlarmListActivity" >
        </activity>
        <activity android:name="com.riandy.flexiblealertscheduler.AlarmScreen" />
        <activity android:name="com.riandy.flexiblealertscheduler.AlarmDetailsActivity" />

        <service
            android:name="com.riandy.flexiblealertscheduler.AlarmService"
            android:enabled="true" />

        <receiver android:name="com.riandy.flexiblealertscheduler.AlarmManagerHelper" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".TestActivity"
            android:label="@string/title_activity_test" >
        </activity>
    </application>

</manifest>